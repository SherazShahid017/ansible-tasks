#- name: Install Git
#  apt:
#    pkg: 
#    - git
#    state: latest
#    #update_cache: true
# 
#- name: Git Clone Repo
#  git:
#    repo: https://github.com/LukeBenson/install-scripts
#    dest: ./git
# 
#- name: Running Docker Script
#  script: /home/sheraz_shahid/ansible-tasks/roles-extension/git/docker_install.sh 
###################################################################################

- name: Install aptitude using apt
  apt: 
    name: aptitude 
    state: latest 
    update_cache: true

- name: Install required system packages
  apt: 
    name: [ 'apt-transport-https', 'ca-certificates', 'curl', 'software-properties-common', 'python3-pip' ] 
    state: latest 
    update_cache: true

- name: Add Docker GPG apt Key
  apt_key:
    url: https://download.docker.com/linux/ubuntu/gpg
    state: present

- name: Add Docker Repository
  apt_repository:
    repo: deb https://download.docker.com/linux/ubuntu bionic stable
    state: present

- name: Update apt and install docker-ce
  apt: 
    update_cache: true 
    name: docker-ce 
    state: latest

- name: Install Docker Module for Python
  pip:
    name: docker

    
